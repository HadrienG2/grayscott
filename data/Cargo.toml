[package]
name = "data"
version = "0.1.0"
edition = "2021"

[dependencies]
hdf5 = "0.8"
safe_arch = { version = "0.6", optional = true }
slipstream = { version = "0.2", optional = true }
ndarray = "0.15"
